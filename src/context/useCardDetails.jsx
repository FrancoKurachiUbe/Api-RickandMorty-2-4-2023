/* import { createContext, useState } from "react";

export const useCardDetails = createContext();

export default (props) => {
const[test, setTest] = useState("Test")

    return(
        <useCardDetails.Provider value={{test, setTest}}>
            {props.children}
        </useCardDetails.Provider>
    )
} */